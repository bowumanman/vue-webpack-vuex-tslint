/*import Vue from 'vue'
import VueI18n from 'vue-i18n'

import en from './th_en'
import zh from './th_zh_cn'
import tw from './th_zh_tw'

Vue.use(VueI18n);

const locales = { en, zh, tw }

// set lang
Vue.config.lang = 'zh'
// set locales
Vue.locale(Vue.config.lang, locales[Vue.config.lang]);

Object.keys(locales).forEach(function (lang) {
  Vue.locale(lang, locales[lang])
})

*/